{
  "name": "eventapp",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ 0xronin/eventapp",
    "create-local": "graph create --node http://localhost:8020/ 0xronin/eventapp",
    "remove-local": "graph remove --node http://localhost:8020/ 0xronin/eventapp",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 0xronin/eventapp",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0",
    "@protofire/subgraph-toolkit": "^0.1.2"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
